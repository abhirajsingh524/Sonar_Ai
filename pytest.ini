[pytest]
# Disable pytest's log capturing to avoid conflicts with Flask test client
# and the logging.basicConfig call in utils/config.py
log_cli = false
log_level = WARNING
addopts = -p no:logging
testpaths = tests
